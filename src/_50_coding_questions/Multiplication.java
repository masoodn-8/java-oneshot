package _50_coding_questions;

public class Multiplication {
    static void main() {
        int number = 8;
        int iterator = 1;
        while(iterator < 11){
            System.out.println(number + " x " + iterator + " = " + (number*iterator));
            iterator++;
        }
    }
}
